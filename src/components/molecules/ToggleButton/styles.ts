import {StyleSheet} from 'react-native';
import { Spacing, Typography } from '../../../styles';


export const styles = StyleSheet.create({
main:{flexDirection: 'row', alignItems: 'center', padding: Spacing.S11},
title:{marginRight: Spacing.S8, fontSize: Typography.FS11}
});
